apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aws-root-app 
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lnaura/Solog-Webapp-CI-CD-Manifests.git 
    targetRevision: HEAD
    path: applications/aws
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true