apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/backend

images:
  - name: solog-backend
    newName: <AWS_ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/solog-backend
    newTag: latest  # 이 태그는 CI가 나중에 업데이트합니다.